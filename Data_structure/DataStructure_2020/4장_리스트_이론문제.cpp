#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
	// 추상자료형 : Abstract data type, ADT
	// 저장된 데이터, 데이터에 대한 작업들, 작업 중 발생 가능한 에러 상황들을 명시한다.

	// 리스트 ADT
	// integer size()
	// boolean isEmpty()
	// iterator elements()

	// 순위기반 리스트 ADT
	// 위치기반 리스트 ADT
	// 예외도 확인할 수 있도록 합니다.
	// "실행 불가능한 작업 때문에 예외를 발령한다.(throw)"

	// 순위기반 리스트 ADT
	// 초기화(initialization)
	// 순회(traversal)
	// 삽입(insertion)
	// 삭제(deletion)
	
	// 단일연결리스트
	// 이중연결리스트
	// 초기화
	// 순회
	// 삽입
	// 삭제

	// grouping And sharing
	// 설계 방안 A: 레코드의 리스트 사용
	// 설계 방안 A의 구현 1:  배열 이용
	// 설계 방안 A의 구현 2:  연결리스트 이용
	// 설계 방안 B: 부리스트들의 리스트 사용
	// 설계 방안 B의 구현 1: 2D 배열 이용
	// 설계 방안 B의 구현 2: 연결리스트의 배열 이용

	// 리스트 확장: 공유
	// 설계 방안 A: 레코드의 리스트 사용
	// 설계 방안 A의 구현 1: 배열 이용
	// 설계 방안 A의 구현 2: 연결리스트 이용

	// 설계 방안 B: 포인터의 리스트 사용

	// 설계 방안 C: 다중리스트 사용
	// 설계 방안 C의 구현 1: 2D 배열 이용
	// 설계 방안 C의 구현 2: 다중연결리스트 이용

	// 아래 응용문제 확인하기!!
	return 0;
}
