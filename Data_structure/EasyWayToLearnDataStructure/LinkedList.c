#include <stdio.h>
#include <stdlib.h>

typedef int element;
typedef struct ListNode {
	element data;
	struct ListNode *link;
}ListNode; // 구조체를 설정합니다.

int main() {
	ListNode *head = NULL;
	return 0;
}
